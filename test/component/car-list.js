/*import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
import CarList from "../app/client/imports/components/car-list/car-list";


const wrapper = mount(<CarList />);

describe("<CarList/>", () => {

  it('should render add-car button', () => {
    expect(wrapper.find('.add-car-button button')).to.have.length(1);
  });

  it('should render car list', () => {
    expect(wrapper.find('.list-cars')).to.have.length(1);
  });

});
*/
