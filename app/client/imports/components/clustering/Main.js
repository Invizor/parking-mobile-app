import React, {PropTypes, Component} from 'react';
import { render } from 'react-dom';
import Layout from './Layout.js';


var ClusterMain = React.createClass({
  render() {
    return (
      <Layout />
    );
  }
});

export default ClusterMain;

